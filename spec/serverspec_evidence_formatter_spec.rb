require "spec_helper"

RSpec.describe ServerspecEvidenceFormatter do
  it "has a version number" do
    expect(ServerspecEvidenceFormatter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
